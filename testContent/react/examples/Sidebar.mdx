---
id: Sidebar
cssPrefix: pf-v6-c-sidebar
section: components
propComponents: ['Sidebar', 'SidebarContent', 'SidebarPanel']
---

## Examples
### Basic

import SidebarBasic from "./SidebarBasic.tsx?raw"

<LiveExample src={SidebarBasic} />

### Stack

import SidebarStack from "./SidebarStack.tsx?raw"

<LiveExample src={SidebarStack} />

### Panel right with gutter

import SidebarPanelRightGutter from "./SidebarPanelRightGutter.tsx?raw"

<LiveExample src={SidebarPanelRightGutter} />

### Sticky panel
A `tabIndex` should be added to the `Sidebar` when there is scrollable content in order for the  overflow content to be accessible by keyboard.

import SidebarStickyPanel from "./SidebarStickyPanel.tsx?raw"

<LiveExample src={SidebarStickyPanel} />

### Static panel
A `tabIndex` should be added to the `Sidebar` when there is scrollable content in order for the  overflow content to be accessible by keyboard.

import SidebarStaticPanel from "./SidebarStaticPanel.tsx?raw"

<LiveExample src={SidebarStaticPanel} />

### Responsive panel width
A `tabIndex` should be added to the `Sidebar` when there is scrollable content in order for the  overflow content to be accessible by keyboard.

import SidebarResponsivePanel from "./SidebarResponsivePanel.tsx?raw"

<LiveExample src={SidebarResponsivePanel} />

### Border

import SidebarBorder from "./SidebarBorder.tsx?raw"

<LiveExample src={SidebarBorder} />

### Padding on panel

import SidebarPaddingPanel from "./SidebarPaddingPanel.tsx?raw"

<LiveExample src={SidebarPaddingPanel} />

### Padding on content

import SidebarPaddingContent from "./SidebarPaddingContent.tsx?raw"

<LiveExample src={SidebarPaddingContent} />
